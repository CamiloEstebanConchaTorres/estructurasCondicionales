# Escriba un programa que reciba como entrada dos números, y los muestre ordenados de menor a mayor:

Numero1 = float(input("ingrese el valor del primer numero \n"))
Numero2 = float(input("ingrese el valor del segundo numero \n"))
Orden = max(Numero1, Numero2)
Orden2 = min(Numero1, Numero2)
print(f"Menor: {Orden2} Mayor: {Orden}")
    
